{
  "name": "gospelsub-pure",
  "version": "1.0.0",
  "description": "gospelsub in web, written in purescript",
  "main": "",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "spago test",
    "build": "spago bundle-app --minify --to public/main.js && esbuild src/import.js --bundle --minify --outdir=public",
    "run": "spago run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.21.3",
    "purescript": "^0.15.2",
    "spago": "^0.20.9"
  },
  "dependencies": {
    "@material/web": "^1.4.1"
  }
}
